{
  "name": "ev-trip-dashboard",
  "private": true,
  "description": "Tests for the EV Trip Dashboard Lovelace strategy and its custom cards. The shipped artifact is still the plain ev-trip-dashboard.js — there is no build step.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}
